import { typeDefs as __typeDefs } from "./typeDefs"
import { resolvers as __resolvers } from "./resolvers"

export namespace File {
  export const typeDefs = __typeDefs
  export const resolvers = __resolvers
}
